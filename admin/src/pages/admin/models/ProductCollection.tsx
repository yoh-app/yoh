import React from 'react';
import PrismaTable from 'components/PrismaTable';

const ProductCollection: React.FC = () => {
  return <PrismaTable model="ProductCollection" />;
};

export default ProductCollection;
