import React from 'react';
import PrismaTable from 'components/PrismaTable';

const VideoCollection: React.FC = () => {
  return <PrismaTable model="VideoCollection" />;
};

export default VideoCollection;
