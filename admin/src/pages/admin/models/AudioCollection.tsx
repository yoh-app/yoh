import React from 'react';
import PrismaTable from 'components/PrismaTable';

const AudioCollection: React.FC = () => {
  return <PrismaTable model="AudioCollection" />;
};

export default AudioCollection;
