import React from 'react';
import PrismaTable from 'components/PrismaTable';

const PageCollection: React.FC = () => {
  return <PrismaTable model="PageCollection" />;
};

export default PageCollection;
