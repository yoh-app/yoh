import React from 'react';
import PrismaTable from 'components/PrismaTable';

const EventCollection: React.FC = () => {
  return <PrismaTable model="EventCollection" />;
};

export default EventCollection;
