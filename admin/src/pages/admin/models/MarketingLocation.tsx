import React from 'react';
import PrismaTable from 'components/PrismaTable';

const MarketingLocation: React.FC = () => {
  return <PrismaTable model="MarketingLocation" />;
};

export default MarketingLocation;
